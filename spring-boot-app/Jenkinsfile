/*
  Jenkins Declarative Pipeline for:
  CI + SonarQube + Docker Build + Push to ACR
*/

pipeline {

  // Docker agent with Maven + Java 17 + Docker CLI
  agent {
    docker {
        image 'maven:3.9.6-eclipse-temurin-17'
        args '-v /var/run/docker.sock:/var/run/docker.sock'
    }
}


  environment {
    SONAR_URL        = 'http://20.220.31.252:9000'
    ACR_LOGIN_SERVER = 'ayushacr.azurecr.io'
    ACR_REPO         = 'springboot-app'
    DOCKER_IMAGE     = "${ACR_LOGIN_SERVER}/${ACR_REPO}:${BUILD_NUMBER}"
  }

  stages {

    stage('Checkout') {
      steps {
        git branch: 'main', url: 'https://github.com/Ayush3366/Project1BC.git'
        sh 'echo "=== CHECKOUT COMPLETE ==="; ls -R'
      }
    }

    stage('Build & Test') {
      steps {
        sh '''
          echo "=== BUILD & TEST STAGE STARTED ==="
          cd spring-boot-app
          mvn clean package
          echo "=== BUILD & TEST STAGE COMPLETED ==="
        '''
      }
    }

    stage('Static Code Analysis (SonarQube)') {
      steps {
        withCredentials([string(credentialsId: 'sonarqube', variable: 'SONAR_AUTH_TOKEN')]) {
          sh '''
            echo "=== SONARQUBE ANALYSIS STAGE STARTED ==="
            cd spring-boot-app
            mvn sonar:sonar \
              -Dsonar.login=$SONAR_AUTH_TOKEN \
              -Dsonar.host.url=${SONAR_URL}
            echo "=== SONARQUBE ANALYSIS STAGE COMPLETED ==="
          '''
        }
      }
    }

      stage('Install Docker CLI in Container') {
          steps {
              sh '''
                  echo "=== INSTALLING DOCKER CLI ==="
                  apt-get update
                  apt-get install -y docker.io
              '''
          }
      }

    
    stage('Build & Push Docker Image to ACR') {
      steps {
        script {
          sh '''
            echo "=== DOCKER BUILD STAGE STARTED ==="
            cd spring-boot-app
            docker build -t ${DOCKER_IMAGE} .
            echo "=== DOCKER BUILD STAGE COMPLETED ==="
          '''

          def dockerImage = docker.image("${DOCKER_IMAGE}")

          docker.withRegistry("https://${ACR_LOGIN_SERVER}", "acr-cred") {
            echo "=== PUSHING IMAGE TO ACR ==="
            dockerImage.push()
            echo "=== IMAGE PUSHED SUCCESSFULLY ==="
          }
        }
      }
    }
  }

  post {
    success { echo "✅ Pipeline completed successfully! Image: ${DOCKER_IMAGE}" }
    failure { echo "❌ Pipeline failed. Check logs above." }
  }
}
